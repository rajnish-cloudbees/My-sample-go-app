apiVersion: automation.cloudbees.io/v1alpha1
kind: action
name: sha-tagged-action
description: Action that uses cloudbees.scm.sha to reference a container image

runs:
  using: composite
  steps:
    - name: Run from SHA-tagged image
      uses: docker://020229604682.dkr.ecr.us-east-1.amazonaws.com/testing/cbp-25252-test:${{ cloudbees.scm.sha }}
      run: |
        echo "=== AC 3: Action DSL ==="
        echo "PASS: cloudbees.scm.sha resolved in action step uses"
