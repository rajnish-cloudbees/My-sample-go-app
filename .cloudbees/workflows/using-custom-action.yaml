apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My automation

on:
  workflow_dispatch: {}

jobs:
  build:
    steps:
      - name: action test
        uses: My-sample-go-app/.cloudbees/actions
        with:
          arg-1: |
            Optimus Prime