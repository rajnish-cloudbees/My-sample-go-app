apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My automation

on:
  workflow_dispatch: {}

jobs:
  build:
    steps:
      - name: action test
        uses: https://github.com/rajnish-cloudbees/test-integration/.cloudbees/actions/custom-action@main
        with:
          arg-1: |
            Optimus Prime
# apiVersion: automation.cloudbees.io/v1alpha1
# kind: workflow
# name: My workflow
# on:
#   workflow_dispatch:
  
# permissions:
#   id-token: write
  
# jobs:
#   build:
#     steps:
          
#       - name: Login to AWS
#         uses: cloudbees-io/configure-aws-credentials@v1
#         id: aws-login
#         with:
#           aws-region: us-east-1
#           role-to-assume: arn:aws:iam::020229604682:role/preprod-cloudbees-infra-mgmt
#           role-duration-seconds: "3600"