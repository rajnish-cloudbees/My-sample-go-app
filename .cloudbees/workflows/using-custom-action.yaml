apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My automation

on:
  workflow_dispatch: {}

jobs:
  build:
    steps:
      - name: action test
        uses: https://github.com/rajnish-cloudbees/My-sample-go-app/@main
        with:
          arg-1: |
            Optimus Prime